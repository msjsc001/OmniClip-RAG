[project]
name = "omniclip-rag"
version = "0.4.9"
description = "Local-first desktop RAG for Markdown and Logseq vaults."
readme = "README.md"
requires-python = ">=3.13"
license = { file = "LICENSE" }
authors = [
  { name = "EllisMorrow" }
]
keywords = ["rag", "markdown", "logseq", "sqlite", "lancedb", "desktop"]
dependencies = [
  "mcp>=1,<2",
  "pypdf>=5.0.0,<6.0.0",
]

[project.urls]
Repository = "https://github.com/EllisMorrow/OmniClip-RAG"
Documentation = "https://msjsc001.github.io/OmniClip-RAG/"
